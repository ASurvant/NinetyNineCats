<table>
  <th>
    <h1><%= @cat.name %></h1>
  </th>
  <tr>
    <td>
      <%= @cat.age %> years old
    </td>
  </tr>
  <tr>
    <td>
      Born on <%= @cat.birth_date %>
    </td>
  </tr>
  <tr>
    <td>
      <%= @cat.color %>
    </td>
  </tr>
  <tr>
    <td>
      <%= @cat.sex == 'M' ? 'Male' : 'Female' %>
    </td>
  </tr>
  <tr>
    <td>
      <%= @cat.description %>
    </td>
  </tr>
</table>

<a href="<%= edit_cat_url %>"><button>Edit your Cat</button></a>
<br><br>

<table>
  <tr>
    <td><h3>Requested Rentals:</h3></td>
  </tr><tr>
    <td><center><u>Start Date</u> | <u>End Date</u> | <u>Status</u></center></td>
  </tr>
  <% @cat.cat_rental_requests.order('start_date').each do |rental| %>
  <tr><td><center>
    <%= rental.start_date %> | <%= rental.end_date %>
    <% if rental.pending? %>
    <br><td>
      <form action="/cat_rental_requests/<%= rental.id %>/approve" method="post"><button>Approve</button></a>
      <form action="/cat_rental_requests/<%= rental.id %>/deny" method="post"><button>Deny</button></a></td>
    <% else %>
     | <%= rental.status %>
    <% end %>
  </center></td></tr>
  <% end %>
</table>
